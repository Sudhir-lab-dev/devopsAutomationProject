apiVersion: v1
kind: Service
metadata:
  name: automation-app
  namespace: default
spec:
  selector:
    app: automation-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer